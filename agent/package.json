{
  "name": "altheia-sui-demo-agent",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Autonomous agent: observes DeepBook, decides, signs with the AgentCap, executes guarded trades, halts on revocation.",
  "scripts": {
    "agent": "tsx agent.ts",
    "demo": "tsx --env-file-if-exists=../.env demo.ts"
  },
  "dependencies": {
    "@altheia-xyz/sui": "^0.0.3"
  },
  "devDependencies": {
    "@types/node": "^26.0.0",
    "tsx": "^4.19.0",
    "typescript": "^5.6.0"
  }
}
